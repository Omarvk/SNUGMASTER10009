"use strict";
describe("SellerDlgController should be unit tested here", function() {
	var ctrl;
	var scope;
	/*
	var mockModalParam = function(seller) {
		return {
				modalParam: function() {
					return seller;
				}
			}
		}
		
	};
	*/
		/*var fakeModal = {
		result: {
			then: function(ok, cancel) {
				this.ok = ok;
				this.cancel = cancel;
			}
		},
		close: function(data) {
			this.result.then(data, "");
		},
		dismiss: function(reason) {
			this.result.then("" ,reason);
		}
	};*/
	/*
	beforeEach(module("project3App"));

	beforeEach(inject(function($controller, $rootScope) {
		scope = $rootScope.$new(); 
		ctrl = $controller("SellerDlgController", {
			$scope: scope,
			locationParams: mockModalParam
		});

	}));

	it("ctrl is defined", function() {
		expect(ctrl).toBeDefined();
	});

	it("sellerobj is defined", function() {
		//expect(sellerobj).toBeDefined();
	});	

	it("expect add to have empty objects", inject(function(){
		//modalParam = {};
		//expect(sellerobj).toEqual({});
		//expect(scope.seller.id).toEqual(null);

	}));
	*/
});